{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.19.0"
  },
  "scripts": {
    "dev": "npx lerna bootstrap & cd .\\packages\\docz & yarn dev",
    "build": "gatsby build",
    "serve": "gatsby serve"
  }
}
